<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.topvision.ems.fault.domain.Level">
	<select id="selectByMap" resultType="level">
		select levelId, name, active, icon from Levels order by levelId desc
	</select>

</mapper>
