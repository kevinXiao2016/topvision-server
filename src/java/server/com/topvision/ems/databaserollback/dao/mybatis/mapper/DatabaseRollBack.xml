<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="DataBaseRollBack">
	<select id="execute" parameterType="map" resultType="map">
		${sql}
	</select>
	<select id="execute2" parameterType="map" resultType="string">
		${sql}
	</select>
	<select id="execute3" parameterType="map" resultType="string">
		${sql}
	</select>
	<update id="executeRollback" parameterType="map">
		${sql}
	</update>
</mapper>