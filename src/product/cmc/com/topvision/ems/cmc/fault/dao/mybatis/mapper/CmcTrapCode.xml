<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.topvision.ems.cmc.fault.domain.CmcTrapCode">		<!-- ################################## select start ####################################### -->	<select id="getCmcCodeFromTrapCode" parameterType="long" resultType="int">		select emsEventTypeId from cmceventtyperelation where deviceEventTypeId = #{deviceEventTypeId}	</select>		<select id="getCcmtsState" parameterType="map" resultType="int">		SELECT B.state FROM oltonurelation A, entitysnap B where A.entityId	=#{entityId} AND A.onuIndex=#{cmcIndex} AND A.onuId=B.entityId	</select> </mapper>