#Generated by ResourceBundle Editor (http://eclipse-rbe.sourceforge.net)
# Properties file with JDBC-related settings.
# Applied by PropertyPlaceholderConfigurer from "applicationContext.xml".
# Targeted at system administrators, to avoid touching the context XML files.

# Acquiring new connections is slow, so eagerly retrieve extra connections
# when current pool size is reached
cpool.acquireIncrement                         = 5
cpool.engine.acquireIncrement                  = 5
# Time to wait for an open connection before timing out(in milliseconds)
cpool.checkoutTimeout                          = 60000
cpool.engine.checkoutTimeout                   = 60000
# c3p0 database pool
# Connection pool size
cpool.initialPoolSize                          = 256
cpool.engine.initialPoolSize                   = 128
# How long to keep unused connections around(in seconds)
# Note: MySQL times out idle connections after 8 hours(28,800 seconds)
# so ensure this value is below MySQL idle timeout
cpool.maxIdleTime                              = 3600
cpool.engine.maxIdleTime                       = 3600
# How long to hang on to excess unused connections after traffic spike (in seconds)
cpool.maxIdleTimeExcessConnections             = 1800
cpool.engine.maxIdleTimeExcessConnections     = 1800
cpool.maxPoolSize                              = 512
cpool.engine.maxPoolSize                       = 256
cpool.minPoolSize                              = 100
cpool.engine.minPoolSize                       = 100

database.type = mysql

# Oracle
#jdbc.driverClassName=oracle.jdbc.driver.OracleDriver
#jdbc.url=jdbc:oracle:thin:@172.17.2.3:1521:orcl
#jdbc.username=epon2
#jdbc.password=epon2
#jdbc.dba.username=system
#jdbc.dba.password=oracle
# mysql
jdbc.driverClassName = com.mysql.jdbc.Driver

#Server Database
jdbc.password        = ems
jdbc.url             = jdbc:mysql://localhost:3003/ems
jdbc.username        = root

#Engine Database
jdbc.engine.password        = ems
jdbc.engine.url             = jdbc:mysql://localhost:3003/ems
jdbc.engine.username        = root
